iOS platform folder placeholder.

If you later run `flutter create` from this folder or add iOS support, Flutter will populate native iOS project files here.

For now this file is a placeholder so the directory exists and you can add native overrides later.
